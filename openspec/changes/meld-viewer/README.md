# meld-viewer

Add MeldViewer composite to @meldui/vue: PDF/image/text/markdown viewer with programmatic annotation API built on EmbedPDF (headless). Phase 1: parity + native annotations. Phase 2: redaction, stamps, signature, forms (opt-in).
