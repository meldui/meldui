---
title: Form
description: Form components with vee-validate integration for validation and error handling.
category: components
subcategory: Form
order: 20
package: '@meldui/vue'
componentName: Form
subComponents:
  - name: FormField
    description: Re-export of vee-validate Field. Provides field context via scoped slot.
  - name: FormFieldArray
    description: Re-export of vee-validate FieldArray for dynamic field lists.
  - name: FormItem
    description: Wrapper for a single form field with auto-generated IDs.
  - name: FormLabel
    description: Label with automatic error state styling.
  - name: FormControl
    description: Wraps the input element with ARIA attributes for accessibility.
  - name: FormDescription
    description: Help text for the field.
  - name: FormMessage
    description: Validation error message (auto-populated by vee-validate).
---

import FormDemo from '../../../demos/form/FormDemo.vue'

## Usage

<FormDemo client:only="vue" />

## Composable

`useFormField()` — provides access to field error state, IDs, and name within a FormItem context.
