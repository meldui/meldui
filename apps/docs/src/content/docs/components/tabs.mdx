---
title: Tabs
description: Tabbed navigation with line and pill variants for organizing content.
category: components
subcategory: Layout
order: 32
package: '@meldui/vue'
componentName: Tabs
props:
  - name: variant
    type: "'line' | 'pill'"
    default: "'line'"
    description: The visual style of the tabs.
  - name: defaultValue
    type: string
    description: The default active tab value.
  - name: modelValue
    type: string
    description: The controlled active tab. Use with v-model.
---

import TabsDemo from '../../../demos/tabs/TabsDemo.vue'
import TabsPillDemo from '../../../demos/tabs/TabsPillDemo.vue'
import TabsVerticalDemo from '../../../demos/tabs/TabsVerticalDemo.vue'

## Usage

<TabsDemo client:only="vue" />

## Pill Variant

<TabsPillDemo client:only="vue" />

## Vertical & Disabled

Use `orientation="vertical"` for vertical layout. Individual triggers can be `disabled`.

<TabsVerticalDemo client:only="vue" />

## Sub-components

| Component     | Description                                                     |
| ------------- | --------------------------------------------------------------- |
| `Tabs`        | Root container. Props: `variant`, `defaultValue`, `orientation` |
| `TabsList`    | Tab button container                                            |
| `TabsTrigger` | Tab button. Props: `value` (required), `disabled`               |
| `TabsContent` | Tab panel. Props: `value` (required)                            |
