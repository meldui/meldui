---
title: Pin Input
description: A PIN code input with individual boxes for each character.
category: components
subcategory: Form
order: 24
package: '@meldui/vue'
componentName: PinInput
props:
  - name: otp
    type: boolean
    default: 'true'
    description: Whether to use OTP mode (one-time password autocomplete).
---

import PinInputDemo from '../../../demos/pin-input/PinInputDemo.vue'
import PinInputNumericDemo from '../../../demos/pin-input/PinInputNumericDemo.vue'

## Usage

<PinInputDemo client:only="vue" />

## Numeric

Use `type="number"` to restrict to digits. The `placeholder` prop customizes empty slot display.

<PinInputNumericDemo client:only="vue" />

## Sub-components

| Component           | Description                                         |
| ------------------- | --------------------------------------------------- |
| `PinInput`          | Root with value state. Extends reka-ui PinInputRoot |
| `PinInputGroup`     | Visual container                                    |
| `PinInputSlot`      | Individual input slot. Props: `index` (required)    |
| `PinInputSeparator` | Separator between slots                             |
