---
title: Pagination
description: Navigation controls for paginated content — page numbers, prev/next, and first/last.
category: components
subcategory: Navigation
order: 45
package: '@meldui/vue'
componentName: Pagination
subComponents:
  - name: PaginationContent
    description: Layout container with items slot
  - name: PaginationItem
    description: Page number button
    props:
      - name: value
        type: number
        required: true
        description: Page number
      - name: isActive
        type: boolean
        description: Active page styling
  - name: PaginationPrevious
    description: Previous page button
  - name: PaginationNext
    description: Next page button
  - name: PaginationFirst
    description: First page button
  - name: PaginationLast
    description: Last page button
  - name: PaginationEllipsis
    description: Collapsed page indicator
---

import PaginationDemo from '../../../demos/pagination/PaginationDemo.vue'
import PaginationSimpleDemo from '../../../demos/pagination/PaginationSimpleDemo.vue'

## Usage

<PaginationDemo client:only="vue" />

## Simple (Previous / Next)

A compact pagination with `siblingCount={0}` and no first/last buttons.

<PaginationSimpleDemo client:only="vue" />

## See also

For paginating tabular or list data with a page-size selector, page info, and
selected-count display, see [`DataPagination`](/docs/components/data-pagination).
That composite mirrors what `<DataTable>` renders in its footer and can be
used independently with grids, card lists, or any custom view.
