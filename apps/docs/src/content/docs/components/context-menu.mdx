---
title: Context Menu
description: A right-click context menu with items, groups, and sub-menus.
category: components
subcategory: Navigation
order: 42
package: '@meldui/vue'
componentName: ContextMenu
---

import ContextMenuDemo from '../../../demos/context-menu/ContextMenuDemo.vue'

## Usage

<ContextMenuDemo client:only="vue" />

## Sub-components

Same structure as [DropdownMenu](/docs/components/dropdown-menu) — supports `ContextMenuCheckboxItem`, `ContextMenuRadioGroup`, `ContextMenuRadioItem`, `ContextMenuLabel`, `ContextMenuShortcut`, `ContextMenuSub`, `ContextMenuSubTrigger`, and `ContextMenuSubContent`.
