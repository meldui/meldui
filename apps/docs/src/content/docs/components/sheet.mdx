---
title: Sheet
description: A panel that slides in from the edge of the screen.
category: components
subcategory: Overlay
order: 52
package: '@meldui/vue'
componentName: Sheet
props:
  - name: side
    type: "'top' | 'right' | 'bottom' | 'left'"
    default: "'right'"
    description: The edge the sheet slides from (set on SheetContent).
---

import SheetDemo from '../../../demos/sheet/SheetDemo.vue'
import SheetSidesDemo from '../../../demos/sheet/SheetSidesDemo.vue'

## Usage

<SheetDemo client:only="vue" />

## Side Variants

Use the `side` prop on `SheetContent` to control which edge the sheet slides from.

<SheetSidesDemo client:only="vue" />

## Sub-components

| Component          | Description                                                           |
| ------------------ | --------------------------------------------------------------------- |
| `Sheet`            | Root state container                                                  |
| `SheetTrigger`     | Opens the sheet                                                       |
| `SheetContent`     | Sliding panel. Props: `side` ('top' \| 'right' \| 'bottom' \| 'left') |
| `SheetHeader`      | Header layout                                                         |
| `SheetTitle`       | Title                                                                 |
| `SheetDescription` | Description                                                           |
| `SheetFooter`      | Footer layout                                                         |
| `SheetClose`       | Close button                                                          |
