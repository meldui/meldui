---
title: Tooltip
description: A popup that displays information when hovering over an element.
category: components
subcategory: Overlay
order: 55
package: '@meldui/vue'
componentName: Tooltip
---

import TooltipDemo from '../../../demos/tooltip/TooltipDemo.vue'
import TooltipSidesDemo from '../../../demos/tooltip/TooltipSidesDemo.vue'

## Usage

<TooltipDemo client:only="vue" />

> **Note:** Wrap your app or layout with `TooltipProvider` once to configure all tooltips.

## Placement

Use the `side` prop on `TooltipContent` to control placement.

<TooltipSidesDemo client:only="vue" />

## Sub-components

| Component         | Description                                                       |
| ----------------- | ----------------------------------------------------------------- |
| `TooltipProvider` | Global configuration. Props: `delayDuration`, `skipDelayDuration` |
| `Tooltip`         | Root state container                                              |
| `TooltipTrigger`  | Element that triggers the tooltip                                 |
| `TooltipContent`  | Tooltip popup. Props: `side`, `sideOffset`, `align`               |
