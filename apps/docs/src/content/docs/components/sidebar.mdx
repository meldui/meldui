---
title: Sidebar
description: A complex sidebar navigation component with collapsible sections, menus, and responsive behavior.
category: components
subcategory: Layout
order: 38
package: '@meldui/vue'
componentName: Sidebar
props:
  - name: side
    type: "'left' | 'right'"
    default: "'left'"
    description: Which side the sidebar appears on.
  - name: variant
    type: "'sidebar' | 'floating' | 'inset'"
    default: "'sidebar'"
    description: The visual style variant.
  - name: collapsible
    type: "'offcanvas' | 'icon' | 'none'"
    default: "'offcanvas'"
    description: How the sidebar collapses.
subComponents:
  - name: SidebarProvider
    description: Root state provider that manages open/closed state.
    props:
      - name: defaultOpen
        type: boolean
        description: Initial open state.
      - name: open
        type: boolean
        description: Controlled open state. Use with v-model.
  - name: SidebarContent
    description: Scrollable content area.
  - name: SidebarHeader
    description: Top section of the sidebar.
  - name: SidebarFooter
    description: Bottom section of the sidebar.
  - name: SidebarGroup
    description: Groups related menu items.
  - name: SidebarGroupLabel
    description: Label heading for a group.
  - name: SidebarGroupContent
    description: Content container within a group.
  - name: SidebarGroupAction
    description: Action button in a group header.
  - name: SidebarMenu
    description: Menu container for menu items.
  - name: SidebarMenuItem
    description: Wrapper for a single menu entry.
  - name: SidebarMenuButton
    description: Clickable menu button with icon and text.
    props:
      - name: variant
        type: "'default' | 'outline'"
        default: "'default'"
        description: Visual style variant.
      - name: size
        type: "'default' | 'sm' | 'lg'"
        default: "'default'"
        description: Button size.
      - name: isActive
        type: boolean
        description: Active state styling.
      - name: tooltip
        type: string | Component
        description: Tooltip text shown on hover (useful in collapsed icon mode).
  - name: SidebarMenuSub
    description: Nested sub-menu container.
  - name: SidebarMenuSubItem
    description: Item within a sub-menu.
  - name: SidebarMenuSubButton
    description: Button within a sub-menu.
    props:
      - name: size
        type: "'sm' | 'md'"
        default: "'md'"
        description: Button size.
      - name: isActive
        type: boolean
        description: Active state styling.
  - name: SidebarMenuAction
    description: Action button within a menu item.
    props:
      - name: showOnHover
        type: boolean
        description: Only show when parent menu item is hovered.
  - name: SidebarMenuBadge
    description: Badge indicator for counts or status.
  - name: SidebarMenuSkeleton
    description: Loading skeleton for menu items.
  - name: SidebarTrigger
    description: Button that toggles the sidebar open/closed.
  - name: SidebarRail
    description: Slim rail visible when sidebar is collapsed.
  - name: SidebarInset
    description: Main content area positioned next to the sidebar.
  - name: SidebarInput
    description: Search input styled for the sidebar.
  - name: SidebarSeparator
    description: Visual separator between groups.
---

import SidebarDemo from '../../../demos/sidebar/SidebarDemo.vue'
import SidebarComplexDemo from '../../../demos/sidebar/SidebarComplexDemo.vue'

## Usage

<SidebarDemo client:only="vue" />

## Complex Example

A full sidebar with collapsible project groups, nested sub-menus (`SidebarMenuSub`), badge counts (`SidebarMenuBadge`), header, footer, and a toggle trigger. Uses `Collapsible` to expand/collapse groups with a chevron indicator.

<SidebarComplexDemo client:only="vue" />

## Composable

`useSidebar()` -- returns `{ state, open, setOpen, openMobile, setOpenMobile, isMobile, toggleSidebar }`.
