---
title: Accordion
description: A vertically stacked set of interactive headings that reveal or hide sections of content.
category: components
subcategory: Layout
order: 30
package: '@meldui/vue'
componentName: Accordion
---

import AccordionDemo from '../../../demos/accordion/AccordionDemo.vue'
import AccordionMultipleDemo from '../../../demos/accordion/AccordionMultipleDemo.vue'

## Usage

<AccordionDemo client:only="vue" />

## Multiple & Disabled

Use `type="multiple"` to allow several items open at once. Individual items can be `disabled`.

<AccordionMultipleDemo client:only="vue" />

## Sub-components

| Component          | Description                                                                 |
| ------------------ | --------------------------------------------------------------------------- |
| `Accordion`        | Root. Props: `type` ('single' \| 'multiple'), `collapsible`, `defaultValue` |
| `AccordionItem`    | Individual section. Props: `value` (required), `disabled`                   |
| `AccordionTrigger` | Clickable header. Slot: `icon` for custom expand indicator                  |
| `AccordionContent` | Collapsible content area                                                    |
