---
title: Button
description: Render an actionable button from an A2UI agent.
category: a2ui
package: '@meldui/a2ui'
order: 7
---

import A2uiDemo from '../../../../demos/a2ui/A2uiDemo.vue'

The **`Button`** renders a clickable button (with a child label and an `action`); `variant` is `primary` (solid CTA), `default` (outline), or `borderless`.

<A2uiDemo client:only="vue" component="Button" />

See the [Catalog Reference](/docs/a2ui/reference) for the full contract, and the [A2UI overview](/docs/a2ui) for how agents target the MeldUI catalog.
