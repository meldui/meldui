---
title: Accordion
description: A collapsible accordion from an A2UI agent.
category: a2ui
package: '@meldui/a2ui'
order: 30
---

import A2uiDemo from '../../../../demos/a2ui/A2uiDemo.vue'

The **`Accordion`** component renders collapsible `items` (each a `title` + `child`), in `single` or `multiple` open mode.

<A2uiDemo client:only="vue" component="Accordion" />

See the [Catalog Reference](/docs/a2ui/reference) for the full contract, and the [A2UI overview](/docs/a2ui) for how agents target the MeldUI catalog.
