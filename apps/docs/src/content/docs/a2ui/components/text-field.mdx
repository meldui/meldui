---
title: TextField
description: Render a data-bound text input from an A2UI agent.
category: a2ui
package: '@meldui/a2ui'
order: 8
---

import A2uiDemo from '../../../../demos/a2ui/A2uiDemo.vue'

The **`TextField`** renders a labelled text input whose `value` is two-way bound to the data model; `variant` selects short/long/number/obscured.

<A2uiDemo client:only="vue" component="TextField" />

See the [Catalog Reference](/docs/a2ui/reference) for the full contract, and the [A2UI overview](/docs/a2ui) for how agents target the MeldUI catalog.
