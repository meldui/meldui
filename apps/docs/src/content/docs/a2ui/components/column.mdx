---
title: Column
description: Arrange components vertically from an A2UI agent.
category: a2ui
package: '@meldui/a2ui'
order: 5
---

import A2uiDemo from '../../../../demos/a2ui/A2uiDemo.vue'

The **`Column`** primitive arranges its `children` (by id) vertically, with `justify` (main axis) and `align` (cross axis).

<A2uiDemo client:only="vue" component="Column" />

See the [Catalog Reference](/docs/a2ui/reference) for the full contract, and the [A2UI overview](/docs/a2ui) for how agents target the MeldUI catalog.
