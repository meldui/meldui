---
title: Clipboard Copy
description: A component for copying text to the clipboard with visual feedback.
category: composites
order: 6
package: '@meldui/vue'
componentName: ClipboardCopy
subComponents:
  - name: ClipboardCopy
    description: Root component. Accepts value (the text to copy) and manages copy state
  - name: ClipboardCopyButton
    description: The trigger button that initiates the copy action
  - name: CopyIdle
    description: Slot content shown when idle (before copying)
  - name: CopySuccess
    description: Slot content shown after a successful copy
---

import ClipboardCopyDemo from '../../../demos/clipboard-copy/ClipboardCopyDemo.vue'

## Usage

<ClipboardCopyDemo client:only="vue" />

## Composable

### `useCopyToClipboard`

A composable for programmatic clipboard access outside of the component tree.

```ts
const { copy, copied } = useCopyToClipboard()

await copy('Hello, world!')
// copied.value === true for a short duration
```
