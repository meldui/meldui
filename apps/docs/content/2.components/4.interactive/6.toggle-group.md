---
title: Toggle Group
description: A group of toggle buttons where one or more can be active.
---

## Usage

::ComponentPreview{name="ToggleGroupBasic"}
::

## API Reference

### ToggleGroup

The root container for a set of toggle items.

| Prop         | Type                        | Default     | Description                                                |
| ------------ | --------------------------- | ----------- | ---------------------------------------------------------- |
| `type`       | `'single' \| 'multiple'`    | `'single'`  | Whether a single or multiple items can be pressed at once. |
| `variant`    | `'default' \| 'outline'`    | `'default'` | The visual style applied to all toggle items.              |
| `size`       | `'default' \| 'sm' \| 'lg'` | `'default'` | The size applied to all toggle items.                      |
| `modelValue` | `string \| string[]`        | —           | The controlled value(s). Use with `v-model`.               |
| `disabled`   | `boolean`                   | `false`     | Whether the entire group is disabled.                      |

#### Events

| Event               | Payload              | Description                              |
| ------------------- | -------------------- | ---------------------------------------- |
| `update:modelValue` | `string \| string[]` | Emitted when the active value(s) change. |

### ToggleGroupItem

An individual toggle button within the group.

| Prop       | Type      | Default | Description                         |
| ---------- | --------- | ------- | ----------------------------------- |
| `value`    | `string`  | —       | A unique value for the toggle item. |
| `disabled` | `boolean` | `false` | Whether this item is disabled.      |

#### Slots

| Slot      | Props | Description                                  |
| --------- | ----- | -------------------------------------------- |
| `default` | —     | The toggle item content (text, icons, etc.). |
