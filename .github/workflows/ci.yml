name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint-and-typecheck:
    name: Lint, Format & Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Biome checks (lint + format)
        run: pnpm check

      # Note: Typecheck temporarily disabled due to pre-existing shadcn-vue Carousel component type issues
      # TODO: Fix Carousel.vue embla-carousel type inference errors
      # - name: Build tabler-vue (required for typecheck)
      #   run: pnpm --filter @meldui/tabler-vue build
      # - name: Type check Vue package
      #   run: pnpm --filter @meldui/vue typecheck

  build:
    name: Build Packages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm --filter @meldui/tabler-vue build && pnpm --filter @meldui/vue build && pnpm --filter @meldui/charts-vue build

      - name: Check for build artifacts
        run: |
          test -f packages/vue/dist/esm/index.mjs || exit 1
          test -f packages/vue/dist/cjs/index.cjs || exit 1
          test -f packages/tabler-vue/dist/esm/index.mjs || exit 1
          test -f packages/tabler-vue/dist/cjs/index.cjs || exit 1
          test -f packages/charts-vue/dist/index.mjs || exit 1
          test -f packages/charts-vue/dist/index.cjs || exit 1
          echo "✅ All build artifacts present"
